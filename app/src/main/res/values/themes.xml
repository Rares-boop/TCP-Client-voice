<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TCPClient" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="BlueButtonStyle" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">#137fec</item>
    </style>

    <style name="DialogSmecher" parent="Theme.Material3.DayNight.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>

        <item name="android:background">@drawable/dialog_bg</item>

        <item name="android:textColorPrimary">#FFFFFF</item> <item name="android:textColorSecondary">#FFFFFF</item> <item name="android:textColor">#FFFFFF</item> <item name="buttonBarPositiveButtonStyle">@style/BlueButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/BlueButtonStyle</item>
        <item name="buttonBarNeutralButtonStyle">@style/BlueButtonStyle</item>
    </style>

    <style name="Theme.TCPClient" parent="Base.Theme.TCPClient" />
</resources>